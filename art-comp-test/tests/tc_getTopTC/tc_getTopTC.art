capsule Top {
    [[rt::impl_preface]]
    `
        #include "lib.art.h"
    `
    /* Ports */
    /* Parts */
    /* Connectors */
    /* State Machine */
    statemachine {
        state State;
        initial -> State 
        `
            ASSERT(libFunc(), "Unexpected return value from library function");
                PASS();
        `;
    };
};
