capsule Top {    
    behavior port timer : Timing;    
    statemachine {        
        choice c;
        state First, Third;        
        a: First -> c on timer.timeout;
        x: c -> Third;
        i: initial -> First
        `
            PASS();
        `;
    };
};

capsule DerivedC : Top {    
    statemachine { // Should NOT get ART_0007 here. Transition "x" is implicitly excluded since "Third" is excluded.
        state Fourth;
        j: c -> Fourth;        
        state exclude Third;  
    };
};