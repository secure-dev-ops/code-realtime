[[rt::decl]]
`
    int libFunc();
`

[[rt::impl]]
`
    int libFunc() {
        return 1;
    }
`

capsule LibCapsule {
    /* Ports */
    /* Parts */
    /* Connectors */
    /* State Machine */
    statemachine {
        state State;
        initial -> State
        `
            // This is the expected version of the capsule
            Log::out << "Hello From Correct Library Capsule" << Log::endl;
        `;
    };
};
