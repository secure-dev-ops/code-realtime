[[rt::decl]]
`
    int libFunc();
`

[[rt::impl]]
`
    int libFunc() {
        return 2;
    }
`

capsule LibCapsule {
    statemachine {
        state State;
        initial -> State
        `
            ASSERT(false, "Unexpected version of LibCapsule was used");
        `;
    };
};
