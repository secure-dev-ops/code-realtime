---
group: tc
---
Test custom include paths enclosed in double quotes. tc.inclusionPaths property contains include paths enclosed in double quotes. It must be properly generated into Makefile. The folder `include` is present inside the project folder. To disable automatic include folder computation, use non-standard name `inclFile.hpp` for an include file.
